import * as userRepository from '../repositories/userRepository.js' 

export default async function deletLike(req,res){
    try {
        const item = res.localItens
        console.log(item)

    } catch (error) {
        
    }
} 